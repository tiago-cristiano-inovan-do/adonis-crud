import BaseSchema from '@ioc:Adonis/Lucid/Schema'

export default class {{pascalCase modelA}} extends BaseSchema {
  protected tableName = '{{modelATable}}'

  public async up () {
    this.schema.table(this.tableName, (table) => {
      table.{{typeKey}}('{{snakeCase columName}}').references('{{modelBTable}}.id')
    })
  }

  public async down () {
    this.schema.table(this.tableName, (table) => {
      table.dropColumn('{{ snakeCase columName}}')
    })
  }
}
