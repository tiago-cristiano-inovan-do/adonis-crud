import BaseController from './BaseController'
import {{repositoryName}} from '@ioc:{{repositoryName}}'
import {{transformerName}} from 'App/Transformers/{{transformerName}}'
import {{pascalCase modelName}}Validator from 'App/Validators/{{pascalCase modelName}}Validator'
import NoValidator from 'App/Validators/NoValidotor'
export default class {{controllerName}} extends BaseController {
  constructor() {
    super([
     {{#each tableFields}}
        {{#if required}}
         '{{this.name}}',
        {{/if}}
    {{/each}}
    ])
    this.repository = {{pascalCase repositoryName}}
    this.validators = {
      store: {
        validatorClass: {{pascalCase modelName}}Validator,
      },
      update: {
        validatorClass: NoValidator,
      },
    }
    this.transformer = {{pascalCase transformerName}}
  }
}
