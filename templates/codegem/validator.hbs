import { schema } from '@ioc:Adonis/Core/Validator'
import { HttpContextContract } from '@ioc:Adonis/Core/HttpContext'

export default class {{pascalCase modelName}}Validator {
  constructor(protected ctx: HttpContextContract) {}

  public schema = schema.create({
    {{#each tableFields}}

      {{#if_eq type 'string'}}
        {{#if required}}
          {{name}}: schema.{{type}}(),
        {{else}}
           {{name}}: schema.string.optional()
        {{/if}}
      {{/if_eq}}

      {{#if_eq type 'number'}}
        {{#if required}}
          {{name}}: schema.{{type}}(),
        {{/if}}
      {{/if_eq}}
    {{/each}}

    // generator_validator
  })

  public messages = {}
}
